import { FC, Fragment } from "react";
import { SectionCard } from "@components/UI";

export const ExploreSivana: FC = () => {
  return (
    <Fragment>
      <SectionCard row="column">
        <h2>Explore Sivana</h2>
      </SectionCard>
    </Fragment>
  );
};
